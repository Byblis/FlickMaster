<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlickMaster">

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ResultActivity" />
        <activity android:name=".TypingActivity" />
        <activity android:name="com.example.testapp.ProfileEditActivity" />
        <activity android:name=".RankingActivity"/>
        <activity android:name=".GardenActivity"/>
        <activity android:name="com.example.flickmaster.StoryModeActivity" />

    </application>

</manifest>

