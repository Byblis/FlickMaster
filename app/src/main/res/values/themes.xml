<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- Theme.App を追加 (Base.Theme.MyApplication を継承) -->
    <style name="Theme.App" parent="Base.Theme.MyApplication"/>
    <!-- FlickMaster のテーマ -->
    <style name="Theme.FlickMaster" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- デフォルトのボタンを MaterialButton に統一 -->
        <item name="buttonStyle">@style/BaseButton</item>
                <!-- 背景色を変更！ -->
        <item name="android:windowBackground">@color/background_color</item>

    </style>

</resources>

